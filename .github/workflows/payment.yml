name: payment CI

on: branch_protection_rule
jobs:
  run-payment:
  runs-on: ubuntu-latest

  steps:
    -name: checkout code
    uses: actions/checkout@v3

    -name: Run python file
    run: python payment.py
